

const hero = (state, action) => {
  switch(action.type) {
    default: return state;
  }
}

const heroes = (state = [], action) => {
  switch(action.type) {
    default: return state;
  }
}

export default heroes
