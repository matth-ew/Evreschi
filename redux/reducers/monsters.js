const monster = (state, action) => {
  switch(action.type) {
    default: return state;
  }
}

const monsters = (state = [], action) => {
  switch(action.type) {
    default: return state;
  }
}

export default monsters
